http {
    server {
        listen 0.0.0.0:8081;
        server_name localhost;
        
        location / {
            root www;
            index index_new.html;
        }
        
        location /uploads/ {
            root www;
            autoindex on;
        }
        
        location /cgi-bin/ {
            root www;
            cgi_path /usr/bin/python3;
            cgi_ext .py;
        }
    }
}
